{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "OpenAiApiKey": "{{ STORED IN USER SECRETS }}",
  "ConnectionStrings": {
    "DefaultConnection": "{{ STORED IN USER SECRETS }}"
  },
  "SigningAuthority": "https://localhost:5003",
  "AllowedHosts": "*",
  "AllowedCORSOrigins": [
    "https://localhost:5003"
  ]
}